<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container"
    android:layout_width="166dp"
    android:orientation="vertical"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_preview_of_film"
        android:layout_width="wrap_content"
        android:layout_height="220dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:adjustViewBounds="true"
        />
    
    <TextView
        android:id="@+id/tv_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text=""
        android:gravity="start"
        android:textColor="@color/black"
        android:maxLines="2"
        android:textSize="16sp"
        android:ellipsize="end"
        android:layout_marginTop="8dp"
        android:paddingBottom="16dp"
        android:fontFamily="@font/roboto_bold"
        app:layout_constraintTop_toBottomOf="@id/iv_preview_of_film"
        app:layout_constraintStart_toStartOf="@id/iv_preview_of_film"
        app:layout_constraintEnd_toEndOf="@id/iv_preview_of_film"
        />

</androidx.constraintlayout.widget.ConstraintLayout>